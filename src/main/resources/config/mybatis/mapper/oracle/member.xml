<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="config.mybatos.mapper.oracle.member">
	<insert id="insertMember" parameterType="memberVo">
		<selectKey keyProperty="no" resultType="int" order="BEFORE" >
			select MEMBER2_SEQ.nextval from dual
		</selectKey>
		insert into member2(no, userid, name, pwd, email1, email2, hp1, hp2, hp3,
			 zipcode, address, addressdetail)
		values(#{no}, #{userid}, #{name}, #{pwd}, #{email1}, #{email2}, #{hp1}, #{hp2},
		 #{hp3}, #{zipcode}, #{address}, #{addressDetail})
	</insert>
	
	<select id="dupUserid" parameterType="String" resultType="int">
		select count(*) from member2 where userid=#{userid}
	</select>
	
	<select id="selectPwd" parameterType="string" resultType="string">
		select pwd from member2 where userid = #{userid} and outdate is null
	</select>
	
	<select id="selectMember" parameterType="string" resultType="membervo">
		select * from member2 where userid = #{userid}
	</select>
	
	<update id="updateMember" parameterType="membervo">
		update member2
		set zipcode=#{zipcode}, address=#{address}, addressdetail=#{addressDetail}, 
		hp1=#{hp1}, hp2=#{hp2}, hp3=#{hp3}, email1=#{email1}, email2=#{email2}
		where userid=#{userid}
	</update>
	
	<update id="memberOut" parameterType="String">
		update member2
		set outdate = sysdate
		where userid = #{userid}
	</update>
</mapper>











